//Authors: Tajinder Thind & Henry Barrisford
//Psuedocode for draw map
//Result apply texture to the map based on the values in the data structure
//passed in
//Parameters: need the data structure containing relative position of map values
// and a data structure containing the textures relating to the map values
//map[][] and texture[]
draw_map()
	foreach row{
		foreach column{
			currentImage = map[row][col]
			draw(texture[currentImage])
		}	
	}
        
}


