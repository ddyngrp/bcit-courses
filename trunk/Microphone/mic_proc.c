#include "win_main.h"

DWORD WINAPI MicProc1(void *arg)
{
	// Thread example from text should be used so we can process messages!

	// Create UDP connection

		// if fails, display error and exit thread

	// Setup waveInHdr
	
		// if fails, display error and exit thread

	// Start reading from microphone

	// Send over network
}

DWORD WINAPI MicProc2(void *arg)
{
}
