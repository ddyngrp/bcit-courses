non-multicast server chosen

if play
	release all semaphores
	while not EOF 
		if client picked a song
			send client song data packets //over udp connection
		else
			send first song on the list data packets

if server_pause
	decrement semaphore on any send threads //stop sending data

if stop/connectoin closed //stop or connection closed
	kill all threads
	WSAcleanup()

if server_request_file
	send listing of songs in music directory to client

//dont know what server_upload is...i would think it would be send the file??

if server_microphone
	//see stephen pseudo code ^_^Y